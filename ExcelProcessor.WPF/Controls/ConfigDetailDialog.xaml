<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ExcelProcessor.WPF.Controls.ConfigDetailDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             MinWidth="800" MinHeight="600" MaxWidth="2000" MaxHeight="1200">

    <UserControl.Resources>
        <!-- 配置项样式 -->
        <Style x:Key="ConfigItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A1A1A" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="2" Opacity="0.3" />
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 配置标签样式 -->
        <Style x:Key="ConfigLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <!-- 配置输入框样式 -->
        <Style x:Key="ConfigInputStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderBrush" Value="#404040" />
            <Setter Property="CaretBrush" Value="{StaticResource CyberBlueBrush}" />
        </Style>

        <!-- 配置下拉框样式 -->
        <Style x:Key="ConfigComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderBrush" Value="#404040" />
        </Style>

        <!-- 操作按钮样式 -->
        <Style x:Key="ConfigActionButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="44" />
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="Margin" Value="0,0,12,0" />
        </Style>

        <!-- 状态指示器样式 -->
        <Style x:Key="StatusIndicatorStyle" TargetType="Border">
            <Setter Property="Width" Value="8" />
            <Setter Property="Height" Value="8" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
    </UserControl.Resources>

    <Border Background="#0F0F0F" 
            CornerRadius="12" 
            BorderBrush="{StaticResource CyberBlueBrush}" 
            BorderThickness="2"
            Effect="{StaticResource MaterialDesignElevationShadow16}">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" 
                    Background="{StaticResource DarkBlueBrush}"
                    CornerRadius="12,12,0,0"
                    Padding="24,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <!-- 左侧图标和标题 -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center">
                        <materialDesign:PackIcon x:Name="TitleIcon"
                                               Kind="Cog" 
                                               Width="24" 
                                               Height="24"
                                               Foreground="{StaticResource CyberBlueBrush}"
                                               VerticalAlignment="Center" />
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="TitleTextBlock"
                                     Text="配置详情" 
                                     FontSize="20" 
                                     FontWeight="Bold"
                                     Foreground="White" />
                            <TextBlock x:Name="SubtitleTextBlock"
                                     Text="编辑配置信息" 
                                     FontSize="13"
                                     Foreground="#CCCCCC"
                                     Margin="0,2,0,0" />
                        </StackPanel>
                    </StackPanel>

                    <!-- 中间状态指示器 -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <Border Style="{StaticResource StatusIndicatorStyle}"
                                Background="{StaticResource NeonGreenBrush}" />
                        <TextBlock Text="配置已就绪" 
                                 FontSize="12"
                                 Foreground="#CCCCCC"
                                 VerticalAlignment="Center" />
                    </StackPanel>
                    
                    <!-- 右侧关闭按钮 -->
                    <Button Grid.Column="2" 
                            x:Name="CloseButton"
                            Style="{StaticResource IconButtonStyle}"
                            Width="36" Height="36"
                            Background="Transparent"
                            BorderBrush="#404040"
                            BorderThickness="1"
                            Click="CloseButton_Click">
                        <materialDesign:PackIcon Kind="Close" 
                                               Width="18" 
                                               Height="18"
                                               Foreground="#CCCCCC" />
                    </Button>
                </Grid>
            </Border>

            <!-- 内容区域 -->
            <Border Grid.Row="1" 
                    Background="#0F0F0F"
                    Margin="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ContentPresenter x:Name="ContentPresenter" />
            </ScrollViewer>
            </Border>

            <!-- 底部按钮栏 -->
            <Border Grid.Row="2" 
                    Background="#1A1A1A"
                    CornerRadius="0,0,12,12"
                    Padding="24,20"
                    BorderBrush="#2A2A2A"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 左侧状态信息 -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Information" 
                                               Width="16" 
                                               Height="16"
                                               Foreground="{StaticResource NeonGreenBrush}"
                                               VerticalAlignment="Center" />
                        <TextBlock x:Name="StatusTextBlock"
                                 Text="配置已保存，可以进行测试" 
                                 FontSize="12"
                                 Foreground="#CCCCCC"
                                 VerticalAlignment="Center"
                                 Margin="8,0,0,0" />
                    </StackPanel>

                    <!-- 右侧操作按钮 -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Right">
                        <Button x:Name="TestButton"
                                Content="测试配置"
                                Style="{StaticResource ConfigActionButtonStyle}"
                                Background="{StaticResource WarningColor}"
                                Foreground="White"
                                Click="TestButton_Click" />
                        <Button x:Name="SaveButton"
                                Content="保存配置"
                                Style="{StaticResource ConfigActionButtonStyle}"
                                Background="{StaticResource SuccessColor}"
                                Foreground="White"
                                Click="SaveButton_Click" />
                        <Button x:Name="CancelButton"
                                Content="取消"
                                Style="{StaticResource ConfigActionButtonStyle}"
                                Background="#2A2A2A"
                                Foreground="#CCCCCC"
                                Margin="0,0,0,0"
                                Click="CancelButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl> 