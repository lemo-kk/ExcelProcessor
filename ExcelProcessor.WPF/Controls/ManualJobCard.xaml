<UserControl x:Class="ExcelProcessor.WPF.Controls.ManualJobCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:ExcelProcessor.WPF.Converters"
             Background="Transparent">

    <UserControl.Resources>
        <converters:CardWidthConverter x:Key="CardWidthConverter" />
        <converters:TextWidthConverter x:Key="TextWidthConverter" />
    </UserControl.Resources>

    <Border Background="#2A2A2A"
            CornerRadius="12"
            Margin="6"
            MinWidth="240"
            MaxWidth="320"
            Height="170"
            Cursor="Hand"
            Effect="{StaticResource MaterialDesignElevationShadow1}"
            MouseLeftButtonDown="ManualJobCard_Click">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3A3A3A" />
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="{StaticResource CyberBlue}" BlurRadius="15" ShadowDepth="2" Opacity="0.4" />
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- 中间：图标和作业名称 -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon x:Name="JobIcon"
                                       Kind="PlayCircle" 
                                       Width="48" 
                                       Height="48"
                                       Foreground="{StaticResource HighlightPurpleBrush}"
                                       HorizontalAlignment="Center" />
                <TextBlock x:Name="JobNameText"
                         Text="作业名称" 
                         FontSize="18" 
                         FontWeight="Bold"
                         Foreground="White"
                         HorizontalAlignment="Center"
                         Margin="0,12,0,0"
                         TextWrapping="Wrap"
                         MaxWidth="{Binding (FrameworkElement.ActualWidth), RelativeSource={RelativeSource AncestorType=Border}, Converter={StaticResource TextWidthConverter}}"
                         TextAlignment="Center"
                         LineHeight="22" />
            </StackPanel>
            
            <!-- 底部：作业描述 -->
            <TextBlock x:Name="JobDescriptionText"
                     Grid.Row="1" 
                     Text="作业描述" 
                     FontSize="11"
                     Foreground="#CCCCCC"
                     HorizontalAlignment="Center"
                     TextWrapping="Wrap"
                     MaxWidth="{Binding (FrameworkElement.ActualWidth), RelativeSource={RelativeSource AncestorType=Border}, Converter={StaticResource TextWidthConverter}}"
                     MaxHeight="30"
                     TextTrimming="CharacterEllipsis"
                     TextAlignment="Center"
                     LineHeight="15"
                     Margin="0,0,0,8" />
            
            <!-- 最底部：标签和状态 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Border Background="#333333" 
                        CornerRadius="6" 
                        Padding="6,3" 
                        Margin="2">
                    <TextBlock x:Name="JobTypeText"
                             Text="作业类型" 
                             FontSize="9" 
                             FontWeight="Medium"
                             Foreground="#CCCCCC" />
                </Border>
                <Border Background="#333333" 
                        CornerRadius="6" 
                        Padding="6,3" 
                        Margin="2">
                    <TextBlock x:Name="JobPriorityText"
                             Text="优先级" 
                             FontSize="9" 
                             FontWeight="Medium"
                             Foreground="#CCCCCC" />
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</UserControl> 