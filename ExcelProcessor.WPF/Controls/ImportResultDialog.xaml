<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="ExcelProcessor.WPF.Controls.ImportResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入结果"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource MaterialDesignPaper}"
        BorderBrush="{StaticResource MaterialDesignDivider}"
        BorderThickness="1"
        TextElement.Foreground="{StaticResource MaterialDesignBody}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,12">
            <TextBlock x:Name="SummaryText" FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap"/>

            <Border Background="#0F000000" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="统计信息" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <WrapPanel>
                        <TextBlock Text="总行数: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="TotalRowsText" Margin="8,0,16,0"/>
                        <TextBlock Text="成功: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="SuccessRowsText" Margin="8,0,16,0"/>
                        <TextBlock Text="失败: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="FailedRowsText" Margin="8,0,16,0"/>
                        <TextBlock Text="跳过: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="SkippedRowsText" Margin="8,0,16,0"/>
                        <TextBlock Text="目标表: " FontWeight="SemiBold"/>
                        <TextBlock x:Name="TargetTableText" Margin="8,0,16,0"/>
                        <TextBlock Text="耗时(秒): " FontWeight="SemiBold"/>
                        <TextBlock x:Name="DurationSecondsText" Margin="8,0,16,0"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <StackPanel>
                <TextBlock x:Name="WarningsHeader" Text="警告信息" FontWeight="SemiBold" Margin="0,0,0,6" Visibility="Collapsed"/>
                <ItemsControl x:Name="WarningsList" Visibility="Collapsed">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="96" Height="32" Margin="0,0,10,0" x:Name="OpenDestinationButton" Content="打开目标" Visibility="Collapsed"/>
            <Button Width="96" Height="32" IsDefault="True" Content="确定" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 