<UserControl x:Class="ExcelProcessor.WPF.Controls.ConfigurationReferencePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExcelProcessor.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Left">
            <Button x:Name="NewReferenceButton" Content="新建引用" Width="100" Height="30" Margin="0,0,10,0" Click="NewReferenceButton_Click"/>
            <Button x:Name="EditReferenceButton" Content="编辑引用" Width="100" Height="30" Margin="0,0,10,0" Click="EditReferenceButton_Click"/>
            <Button x:Name="DeleteReferenceButton" Content="删除引用" Width="100" Height="30" Margin="0,0,10,0" Click="DeleteReferenceButton_Click"/>
            <Button x:Name="ExecuteReferenceButton" Content="执行引用" Width="100" Height="30" Margin="0,0,10,0" Click="ExecuteReferenceButton_Click"/>
            <Button x:Name="RefreshButton" Content="刷新" Width="80" Height="30" Margin="0,0,10,0" Click="RefreshButton_Click"/>
        </StackPanel>

        <!-- 搜索栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
            <TextBox x:Name="SearchTextBox" Width="200" Height="30" Margin="0,0,10,0" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" FontSize="12"/>
            <Button x:Name="SearchButton" Content="搜索" Width="60" Height="30" Click="SearchButton_Click"/>
        </StackPanel>

        <!-- 引用列表 -->
        <DataGrid x:Name="ReferencesDataGrid" Grid.Row="1" Margin="10" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" SelectionChanged="ReferencesDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="引用名称" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="引用类型" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="引用的配置" Binding="{Binding ReferencedConfigName}" Width="150"/>
                <DataGridTextColumn Header="状态" Binding="{Binding IsEnabled}" Width="80"/>
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt}" Width="120"/>
                <DataGridTextColumn Header="更新时间" Binding="{Binding UpdatedAt}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 新建/编辑引用对话框 -->
        <Popup x:Name="ReferenceDialog" IsOpen="False" Placement="Center" 
               PlacementTarget="{Binding ElementName=ReferencesDataGrid}" 
               Width="600" Height="500">
            <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 对话框标题 -->
                    <TextBlock Grid.Row="0" x:Name="DialogTitle" Text="新建配置引用" 
                               FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                    <!-- 表单内容 -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="引用名称:" Margin="0,0,0,5"/>
                            <TextBox x:Name="ReferenceNameTextBox" Height="30" Margin="0,0,0,15"/>

                            <TextBlock Text="引用描述:" Margin="0,0,0,5"/>
                            <TextBox x:Name="ReferenceDescriptionTextBox" Height="30" Margin="0,0,0,15"/>

                            <TextBlock Text="引用类型:" Margin="0,0,0,5"/>
                            <ComboBox x:Name="ReferenceTypeComboBox" Height="30" Margin="0,0,0,15" 
                                      SelectionChanged="ReferenceTypeComboBox_SelectionChanged"/>

                            <TextBlock Text="引用的配置:" Margin="0,0,0,5"/>
                            <ComboBox x:Name="ReferencedConfigComboBox" Height="30" Margin="0,0,0,15"/>

                            <TextBlock Text="覆盖参数 (JSON格式):" Margin="0,0,0,5"/>
                            <TextBox x:Name="OverrideParametersTextBox" Height="80" Margin="0,0,0,15" 
                                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

                            <CheckBox x:Name="IsEnabledCheckBox" Content="启用引用" Margin="0,0,0,15" IsChecked="True"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- 按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="SaveButton" Content="保存" Width="80" Height="30" Margin="0,0,10,0" Click="SaveButton_Click"/>
                        <Button x:Name="CancelButton" Content="取消" Width="80" Height="30" Click="CancelButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl> 